
import AppContainer from './app-container';
import AppRouter from './app-router';

const App: React.FC = () => {
    return (
        <AppContainer>
            <AppRouter/>
        </AppContainer>
    );
};

export default App;
